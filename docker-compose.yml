services:
  exercise:
    restart: unless-stopped
    build: .
    image: exercise:latest
    container_name: exercise
    command: ./bin/boot_exercise.sh
    env_file: ./env/exercise.env
    depends_on:
      - db
    volumes:
      - static_volume:/exercise/exercise/staticfiles
      - media_volume:/exercise/exercise/media
      - logs_volume:/exercise/logs
    networks:
      - network

  db:
    restart: unless-stopped
    image: postgres:17
    container_name: exercise_db
    env_file: ./env/exercise_db.env
    volumes:
      - pgdb-data:/var/lib/postgresql/data
    networks:
      - network

  caddy:
    image: caddy:latest
    container_name: caddy
    env_file:
      - ./env/caddy.env
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
      - static_volume:/exercise/exercise/staticfiles
      - media_volume:/exercise/exercise/media
    networks:
      - network
    depends_on:
      - exercise

volumes:
  static_volume:
  media_volume:
  caddy_data:
  caddy_config:
  logs_volume:
  pgdb-data:
    driver: local

networks:
  network:
    ipam:
      driver: default
      config:
        - subnet: '172.28.0.0/24'
